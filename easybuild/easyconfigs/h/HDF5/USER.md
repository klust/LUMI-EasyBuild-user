# HDF5 user information

These are HDF5 libraries that are a slightly newer release than those offered by HPE.
They cannot be used together with the `cray-hdf5` modules and are also not a plug-in
replacement for those libraries that can be used with the compiler wrappers.

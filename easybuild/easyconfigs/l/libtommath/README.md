# libtommath technical information

In EasyBuild, libtommath is a dependency of newer versions of Tcl. However, Tcl build one internally,
so we did not copy this,

TODO: Currently untested and unfinished, checksum is wrong.

